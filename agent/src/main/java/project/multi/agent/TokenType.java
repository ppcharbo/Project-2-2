package project.multi.agent;

public enum TokenType {
    // Keywords
    S_RULE,
    ACTION_RULE,
    SCHEDULE_RULE,
    TIMEEXPRESSION_RULE,
    TIME,
    LOCATION,
    PRO,
    ROOM,
    DAY,

    // Other tokens
    ERROR,
    EOF
}
